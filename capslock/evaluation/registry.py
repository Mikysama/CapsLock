"""Registry and provenance for tunable behavioural metrics."""

from __future__ import annotations

from dataclasses import asdict, dataclass
from datetime import date

from .. import behavior_defaults as defaults


@dataclass(frozen=True)
class MetricDefinition:
    path: str
    value: int | float
    kind: str
    minimum: int | float
    maximum: int | float
    safety_hard_limit: bool
    evaluation_id: str
    evidence_date: str
    rationale: str


_UNEVALUATED = "baseline-v1"
_DATE = date(2026, 8, 28).isoformat()

METRICS = (
    MetricDefinition(
        "runtime.max_tool_rounds",
        defaults.DEFAULT_MAX_TOOL_ROUNDS,
        "integer",
        1,
        64,
        False,
        _UNEVALUATED,
        _DATE,
        "Conservative multi-step task budget.",
    ),
    MetricDefinition(
        "providers.timeout_seconds",
        defaults.DEFAULT_PROVIDER_TIMEOUT_SECONDS,
        "number",
        1,
        600,
        False,
        _UNEVALUATED,
        _DATE,
        "Provider request deadline.",
    ),
    MetricDefinition(
        "tools.max_read_concurrency",
        defaults.DEFAULT_MAX_READ_CONCURRENCY,
        "integer",
        1,
        32,
        False,
        _UNEVALUATED,
        _DATE,
        "Bounded read-only parallelism.",
    ),
    MetricDefinition(
        "tools.max_argument_repair_attempts",
        defaults.DEFAULT_MAX_ARGUMENT_REPAIR_ATTEMPTS,
        "integer",
        0,
        2,
        False,
        _UNEVALUATED,
        _DATE,
        "Schema/tool-name repair budget.",
    ),
    MetricDefinition(
        "context.trigger_ratio",
        defaults.DEFAULT_CONTEXT_TRIGGER_RATIO,
        "number",
        0.01,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Context compaction trigger.",
    ),
    MetricDefinition(
        "context.target_ratio",
        defaults.DEFAULT_CONTEXT_TARGET_RATIO,
        "number",
        0.01,
        0.99,
        False,
        _UNEVALUATED,
        _DATE,
        "Context compaction target.",
    ),
    MetricDefinition(
        "context.preserve_recent_turns",
        defaults.DEFAULT_CONTEXT_PRESERVE_RECENT_TURNS,
        "integer",
        1,
        64,
        False,
        _UNEVALUATED,
        _DATE,
        "Recent turn retention.",
    ),
    MetricDefinition(
        "context.preserve_recent_tokens",
        defaults.DEFAULT_CONTEXT_PRESERVE_RECENT_TOKENS,
        "integer",
        1024,
        131072,
        False,
        _UNEVALUATED,
        _DATE,
        "Recent token retention.",
    ),
    MetricDefinition(
        "context.max_compaction_failures",
        defaults.DEFAULT_CONTEXT_MAX_COMPACTION_FAILURES,
        "integer",
        1,
        16,
        False,
        _UNEVALUATED,
        _DATE,
        "Compaction retry budget.",
    ),
    MetricDefinition(
        "loop_detection.consecutive_repeats",
        defaults.DEFAULT_LOOP_CONSECUTIVE_REPEATS,
        "integer",
        2,
        16,
        False,
        _UNEVALUATED,
        _DATE,
        "Repeated-call stop threshold.",
    ),
    MetricDefinition(
        "loop_detection.failed_retries",
        defaults.DEFAULT_LOOP_FAILED_RETRIES,
        "integer",
        2,
        16,
        False,
        _UNEVALUATED,
        _DATE,
        "Failed-call stop threshold.",
    ),
    MetricDefinition(
        "loop_detection.cycle_repetitions",
        defaults.DEFAULT_LOOP_CYCLE_REPETITIONS,
        "integer",
        2,
        16,
        False,
        _UNEVALUATED,
        _DATE,
        "Cycle stop threshold.",
    ),
    MetricDefinition(
        "loop_detection.max_cycle_length",
        defaults.DEFAULT_LOOP_MAX_CYCLE_LENGTH,
        "integer",
        2,
        16,
        False,
        _UNEVALUATED,
        _DATE,
        "Largest detected cycle.",
    ),
    MetricDefinition(
        "memory.recall_limit",
        defaults.DEFAULT_MEMORY_RECALL_LIMIT,
        "integer",
        1,
        20,
        False,
        _UNEVALUATED,
        _DATE,
        "Maximum recalled memories.",
    ),
    MetricDefinition(
        "memory.recall_bytes",
        defaults.DEFAULT_MEMORY_RECALL_BYTES,
        "integer",
        1024,
        65536,
        False,
        _UNEVALUATED,
        _DATE,
        "Memory context byte budget.",
    ),
    MetricDefinition(
        "memory.semantic_threshold",
        defaults.DEFAULT_MEMORY_SEMANTIC_THRESHOLD,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Semantic candidate gate.",
    ),
    MetricDefinition(
        "memory.recall_threshold",
        defaults.DEFAULT_MEMORY_RECALL_THRESHOLD,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final recall-score gate.",
    ),
    MetricDefinition(
        "memory.retrieval_weight",
        defaults.DEFAULT_MEMORY_RETRIEVAL_WEIGHT,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final-score retrieval weight.",
    ),
    MetricDefinition(
        "memory.scope_weight",
        defaults.DEFAULT_MEMORY_SCOPE_WEIGHT,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final-score scope weight.",
    ),
    MetricDefinition(
        "memory.confidence_weight",
        defaults.DEFAULT_MEMORY_CONFIDENCE_WEIGHT,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final-score confidence weight.",
    ),
    MetricDefinition(
        "memory.freshness_weight",
        defaults.DEFAULT_MEMORY_FRESHNESS_WEIGHT,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final-score freshness weight.",
    ),
    MetricDefinition(
        "memory.source_validity_weight",
        defaults.DEFAULT_MEMORY_SOURCE_VALIDITY_WEIGHT,
        "number",
        0,
        1,
        False,
        _UNEVALUATED,
        _DATE,
        "Final-score source-validity weight.",
    ),
    MetricDefinition(
        "agents.max_children",
        defaults.DEFAULT_AGENT_MAX_CHILDREN,
        "integer",
        1,
        32,
        False,
        _UNEVALUATED,
        _DATE,
        "Child-agent fan-out.",
    ),
    MetricDefinition(
        "agents.max_concurrency",
        defaults.DEFAULT_AGENT_MAX_CONCURRENCY,
        "integer",
        1,
        32,
        False,
        _UNEVALUATED,
        _DATE,
        "Child-agent parallelism.",
    ),
    MetricDefinition(
        "agents.max_depth",
        defaults.DEFAULT_AGENT_MAX_DEPTH,
        "integer",
        1,
        1,
        True,
        _UNEVALUATED,
        _DATE,
        "Architectural safety boundary; excluded from tuning.",
    ),
    MetricDefinition(
        "agents.max_child_tool_rounds",
        defaults.DEFAULT_AGENT_MAX_CHILD_TOOL_ROUNDS,
        "integer",
        1,
        64,
        False,
        _UNEVALUATED,
        _DATE,
        "Child-agent execution budget.",
    ),
)


def registry_document() -> list[dict[str, object]]:
    return [asdict(item) for item in METRICS]


def baseline_values() -> dict[str, int | float]:
    return {item.path: item.value for item in METRICS}
