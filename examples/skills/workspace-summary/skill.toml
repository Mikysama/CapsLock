schema_version = 1
name = "workspace-summary"
version = "1.0.0"
description = "Summarize a workspace area using read-only local evidence."
min_capslock_version = "1.5.0"
instructions = "instructions.md"
input_schema = "schemas/input.json"
output_schema = "schemas/output.json"
required_tools = ["list_files", "read_file", "search_files", "git_status"]
required_permissions = ["workspace.read"]
